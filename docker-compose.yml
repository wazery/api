api:
  build: .
  command: bundle exec rails s -p 3000 -b '0.0.0.0'
  ports: 
    - "3000:3000"
  links: 
    - database
  volumes:
    - .:/hackhub

database:
  image: mongo
  ports: 
    - "27017:27017"

