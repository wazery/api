<%= fetch(:rails_env) %>:
  clients:
    default:
      database: <%= "#{fetch(:application)}_#{fetch(:rails_env)}" %>
      hosts:
        - localhost:27017
      options:
  options:
test:
  clients:
    default:
      database: api_test
      hosts:
        - localhost:27017
      options:
        read:
          mode: primary
        max_pool_size: 1
